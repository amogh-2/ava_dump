# ava_dump
Avadump is a Rust-based network traffic collection and processing framework built on libpcap. It live captures raw packets and transforms them into structured outputs such as flow summaries, feature vectors, logs, or PCAP files, using configurable and extensible processing pipelines for use cases like network monitoring and intrusion detection.
